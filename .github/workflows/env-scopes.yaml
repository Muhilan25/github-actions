name: 12 env scopes
on: workflow_dispatch
env:
  name: muhilan

jobs:
    job1:
        runs-on: ubuntu-latest
        env:
            name: munna
        steps:
            - name: user workflow
              run: echo "my name is = $name"
