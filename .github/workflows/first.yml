name: my first workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout_repo
              uses: actions/checkout@v7

            - name: list all files
              run: ls -ltra

            - name: executing shell script
              run: ./ascii-script.sh

