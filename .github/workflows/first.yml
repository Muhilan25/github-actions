name: my first workflow
on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout_repo
              uses: actions/checkout@v7

            - name: welcome
              run: echo "my first message"

            - name: list files
              run: ls
              
            - name: read file
              run: cat README.md